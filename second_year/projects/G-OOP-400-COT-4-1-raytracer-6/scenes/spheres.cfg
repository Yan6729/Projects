# spheres.cfg — sous-scène importable
# Contient uniquement des primitives, pas de camera ni lights
# (seule la section primitives est lue lors d'un import)

primitives:
{
    spheres = (
        { x = -40; y = 20; z = -10; r = 20;
          color = { r = 64; g = 200; b = 64; }; },
        { x = 40;  y = 0;  z = -30; r = 15;
          color = { r = 64; g = 64;  b = 220; }; }
    );
};
