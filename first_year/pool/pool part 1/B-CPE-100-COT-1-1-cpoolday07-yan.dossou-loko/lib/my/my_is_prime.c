/*
** EPITECH PROJECT, 2024
** my_is_prime.c
** File description:
** my_is_prime.c
*/
#include <stdio.h>
#include <unistd.h>

int my_is_prime(int nb)
{
    int a;

    if (nb % a == 0) {
        return (0);
    }
    if (nb % a != 0 || nb % nb == 0) {
        return (1);
    }
}
